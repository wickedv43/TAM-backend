package transaction_status

// ENUM(
// PROCESSING,
// DONE,
// )
type TransactionStatus int

// Enum is an alias for TransactionStatus to maintain compatibility with generated enum code
type Enum = TransactionStatus
