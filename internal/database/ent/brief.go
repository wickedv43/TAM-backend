// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/wickedv43/TAM-backend/internal/database/ent/brief"
)

// Brief is the model entity for the Brief schema.
type Brief struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BriefQuery when eager-loading is set.
	Edges        BriefEdges `json:"edges"`
	selectValues sql.SelectValues
}

// BriefEdges holds the relations/edges for other nodes in the graph.
type BriefEdges struct {
	// CustomerBriefs holds the value of the customer_briefs edge.
	CustomerBriefs []*CustomerBrief `json:"customer_briefs,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CustomerBriefsOrErr returns the CustomerBriefs value or an error if the edge
// was not loaded in eager-loading.
func (e BriefEdges) CustomerBriefsOrErr() ([]*CustomerBrief, error) {
	if e.loadedTypes[0] {
		return e.CustomerBriefs, nil
	}
	return nil, &NotLoadedError{edge: "customer_briefs"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Brief) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case brief.FieldCreatedAt, brief.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case brief.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Brief fields.
func (_m *Brief) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case brief.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case brief.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case brief.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Brief.
// This includes values selected through modifiers, order, etc.
func (_m *Brief) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCustomerBriefs queries the "customer_briefs" edge of the Brief entity.
func (_m *Brief) QueryCustomerBriefs() *CustomerBriefQuery {
	return NewBriefClient(_m.config).QueryCustomerBriefs(_m)
}

// Update returns a builder for updating this Brief.
// Note that you need to call Brief.Unwrap() before calling this method if this Brief
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Brief) Update() *BriefUpdateOne {
	return NewBriefClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Brief entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Brief) Unwrap() *Brief {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Brief is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Brief) String() string {
	var builder strings.Builder
	builder.WriteString("Brief(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Briefs is a parsable slice of Brief.
type Briefs []*Brief
